// Bump this string whenever the agreement's real legal text changes.
// A professional who accepted an older version is treated as needing to
// re-accept — this is what makes versioning meaningful rather than
// cosmetic.
export const CURRENT_AGREEMENT_VERSION = 'v1'